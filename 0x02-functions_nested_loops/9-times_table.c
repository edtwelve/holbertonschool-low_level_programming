#include "holberton.h"
void times_table(void)
{
   int j,i,n;
    n  = 9;
    m = i * j;
    for(i=0;i<=9;i++)
    {
     for(j=1;j<=n;j++)
        {
        if (j<=n-1)
        _putchar((i*j) + '0');
        else
	_putchar((i*j) + '0');
	    }
    _putchar("\n");
    }
} 
