#include "holberton.h"
/**
 *_abs: compute absolute value of integer
 *
 *
 * Return: absolute value of i
 * @n: number to evaluate
 */

int _abs(int n)
{

if (n < 0)
        {
        return (-n);
        }
else
        {
        return (n);
        }
return (0);
}
